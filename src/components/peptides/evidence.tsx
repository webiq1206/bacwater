import { Info } from "lucide-react";
import type { EvidenceClass } from "@/lib/calc/peptides";
export function EvidenceBadge(_props: {evidence: EvidenceClass}) {return <span className="inline-flex items-center gap-2 rounded-full border px-3 py-2 text-xs"><Info className="h-4 w-4" aria-hidden="true"/>Verify the exact product and formulation</span>;}
export function WhatNobodyKnows({compound}: {compound:string;evidence:EvidenceClass}) {return <section className="mt-6 rounded-2xl border bg-surface p-6"><h2 className="text-xl font-serif">What this calculator cannot verify</h2><p className="mt-3 leading-relaxed">A result labeled {compound} does not test the identity, purity or amount in a container. It cannot establish compatibility, an appropriate treatment or a storage period. A study or finished-product label does not automatically apply to another formulation sold under a similar name.</p></section>;}
