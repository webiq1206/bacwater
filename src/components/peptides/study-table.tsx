import { ExternalLink } from "lucide-react";
import type { CompoundStudies } from "@/lib/peptides/studies";
export function StudyTable({data}:{data:CompoundStudies}){return <section id="studies" className="mt-14 scroll-mt-24" aria-labelledby="studies-heading">
 <h2 id="studies-heading" className="text-2xl sm:text-3xl font-serif font-medium tracking-tight">What published research looked at</h2><p className="mt-3 text-sm text-muted-foreground leading-relaxed">{data.humanEvidence} {data.fdaStatus}</p>
 <p className="mt-2 text-xs text-muted-foreground">Source abstracts checked September 21, 2026 where available. Read the original methods before interpreting an experimental condition.</p>
 <ul className="mt-6 space-y-4">{data.studies.map((s,i)=><li key={i} className="min-w-0 rounded-2xl border bg-card p-5 sm:p-6 [overflow-wrap:anywhere]"><p className="text-base leading-relaxed">{s.gloss}</p><dl className="mt-4 grid grid-cols-1 gap-x-4 gap-y-3 text-sm min-[380px]:grid-cols-2 sm:grid-cols-5">{[["Tested on",s.species],["Amount",s.amount],["How often",s.frequency],["How long",s.duration],["Route",s.route]].map(([label,value])=><div key={label} className="min-w-0"><dt className="text-xs text-muted-foreground">{label}</dt><dd className="mt-1">{value}</dd></div>)}</dl><a href={s.sourceUrl} target="_blank" rel="noopener noreferrer" className="mt-4 inline-flex min-h-11 max-w-full items-center gap-2 text-sm underline"><ExternalLink aria-hidden="true" className="h-4 w-4 shrink-0"/><span className="min-w-0">{s.sourceTitle}</span></a></li>)}</ul>
 <p className="mt-4 rounded-xl border p-4 text-sm leading-relaxed">These are study details, not instructions. An amount given to animals cannot be turned into a safe amount for a person. These selected sources are not a qualified clinical review of this website or an assessment of your product.</p>
 </section>;}
