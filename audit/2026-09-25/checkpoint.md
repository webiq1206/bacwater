# Audit checkpoint: September 25, 2026

Baseline repository: ca96b4eea9f19696ec8ca3eafba5a36234361d73. Public domain: https://bacwater.ai. Deployment owner uses the existing Replit project BAC Water. GitHub main, its checkout and the production deployment are separate states.

## Implemented batch

- Centralized learning-facet index policy, metadata, visible introductions and schema. Excluded the safety type alternate from the XML sitemap.
- Added the complete product directory to shared public discovery. Grouped llms.txt links and disclosed the affiliate model without an indexing or citation promise.
- Kept all 50 products on the homepage, in the directory and in calculator pickers. Replaced repeated interior-page product carousels with a compact directory link on relevant reference pages. Account, support and policy pages keep their main task in focus.
- Rewrote label-tool copy to match current dimensions, fields, PDF behavior and user-supplied dates. Corrected the limits article's unsupported legal and product claims and instruction to change dilution for a convenient mark. Preserved its section anchors.
- Corrected Google source-preference promises using current Google help, and removed stale claims that the site has no affiliate relationship. Corrected the disclaimer's obsolete direct-retail description.
- Preserved /plan/new as a working alternate, canonicalized its identical builder to /plan and excluded the alternate from discovery exports.
- Added durable, transactional registration/contact/sign-in limits using the existing RequestBudget table. Stored counters omit raw identity and use an HMAC. Saved-message retries retain idempotency.
- New registrations use 15-character minimum passphrases, bounded bcrypt input, normalized names/emails and useful partial-success recovery. Existing six-character passwords remain accepted at sign-in. Added persistent accessible form errors and clarified contact confirmation.
- Corrected deployment documentation: .replit verifies schema; it does not apply schema changes or editorial revisions.

## Follow-through batch

The per-page content review found the old unsupported vial warning repeated across every compound reference, stale label-tool summaries in related-reading cards and ambiguous CJC-1295 naming. The follow-through batch corrects all affected references, preserves the no-DAC qualifier, makes chart captions explicit about illustrative final volume, avoids repeated FAQ question headings and marks the current mobile navigation destination. Privacy copy now documents short-lived abuse counters.

The full browser inventory now includes peptide-filter URLs, search and label utilities. Changed label, preference, limits, registration, directory and topic templates receive additional responsive and alternate-engine checks. Navigation timing measurements are lab observations, not field Core Web Vitals.

## Verification state at this checkpoint

The live HTTP inventory contains 169 public and utility URLs, plus discovery endpoints and four invalid-route probes. All regular URLs returned 200 and all four invalid routes returned 404. This is baseline evidence, not verification that the new code is deployed.

All existing assertion suites and the new targeted regression suite passed locally using Node's tsx import loader. TypeScript and the Webpack production build passed. The tsx CLI cannot create its IPC socket in this workspace, so local tests used `node --import tsx` without changing application behavior. Browser and database workflows must pass for the new commit before release acceptance.

Private search exports, content assessments, keyword map, complete page/task inventory and scope dispositions belong to the private handoff. No private query exports, customer records or credentials are committed.

## Outstanding production and external acceptance

Use a fast-forward checkout update and normal Republish after preserving unrelated changes. Verify the actual production commit and recrawl after publication. Do not publish an older workspace over this batch.

GA4 receiving access is not connected; Bing Webmaster Tools and Ahrefs are unavailable. Optional analytics remains disabled. No real customer email, supplier purchase, new local profile, social post, outreach, paid service or recurring job is authorized or performed by this batch. The online-only model does not establish local-profile eligibility.

Physical device, keyboard-open and assistive-technology testing, operational backup restoration, provider inbox delivery, attribution credit and real field performance remain separate acceptance items. Automated viewport checks do not establish these outcomes.
