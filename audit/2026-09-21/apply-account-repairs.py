from pathlib import Path
import hashlib
changes = {
'.env.example': {'sha': 'f9ac1d7769718ca85427071f5703c8c5867720d5c820eb212d620fc475ca342e', 'edits': [(45,45,'\n# Request protection: set an IP header only after verifying the host overwrites it.\n# Otherwise a device cookie supplements account limits, not a trusted identity.\nTRUSTED_CLIENT_IP_HEADER=""\nAI_REQUESTS_PER_HOUR="100"\n# AI explanations also require an explicitly selected ANTHROPIC_MODEL.\n')]},
'next.config.ts': {'sha':'6c4dab18b13b13a5af9e5dde2bf844a1d881ae99a5fe17814e7b6a320023132a','edits':[(121,121,'      { source: "/:path(forgot-password|reset-password)", headers: [\n        { key: "Cache-Control", value: "private, no-store" },\n        { key: "Referrer-Policy", value: "no-referrer" },\n        { key: "X-Robots-Tag", value: "noindex, nofollow, noarchive" },\n      ] },\n')]},
'package.json': {'sha':'85b20d453e76eee7b9b6a5b6691e40ee5915ec8c0756939aca36aac4662b8c18','edits':[(6,7,'    "test": "tsx src/lib/calc/__tests__/calc.test.ts && tsx src/lib/ai/__tests__/guardrails.test.ts && tsx src/lib/__tests__/workspaces.test.ts && tsx src/lib/__tests__/audit-security.test.ts && tsx src/lib/__tests__/audit-calculation.test.ts && tsx src/lib/ai/__tests__/request.test.ts",\n')]},
'prisma/schema.prisma': {'sha':'3345e20e1023bf91df669e0b4e69aa26b22eb51fc26ff1bc37471f2cf2e49784','edits':[(23,23,'  authVersion    Int        @default(0)\n'),(280,280,'\n// HMAC keys only: no plaintext email addresses, IP addresses or browser identifiers.\nmodel RateLimitBucket {\n  key       String   @id\n  hits      Int      @default(1)\n  expiresAt DateTime\n  @@index([expiresAt])\n}\n')]},
'scripts/audit-journeys.mjs': {'sha':'69f8f276b629b620176f64a38e24c41419d46f1a33cc2fbcfdba26c952e2e5da','edits':[(101,102,"    await page.getByLabel('Password (15+ characters)').fill('FixtureOnly!93842');\n")]},
'scripts/master-audit.mjs': {'sha':'347f9b9b8089c33982f9186f039c3dec764edcdef04ad7bb94b2dad143d8f656','edits':[(50,51,"for (const p of ['/', '/sitemap', '/signin', '/signup', '/forgot-password', '/reset-password', '/plans', '/plan', '/plan/new', '/tools', '/learn', '/peptides']) add(p, 'source-route');\n")]},
'src/app/privacy/page.tsx': {'sha':'37b1ef14e29b6818ce780373d4a97d8fa7149232b02c84dbfa45f1ec8dfc5022','edits':[(8,9,'<h2 className="text-2xl font-serif">Account recovery and abuse protection</h2><p>Recovery links expire after 30 minutes and work once. Only a hash of the recovery token is stored. Resetting a password invalidates previous sessions. Rate-limit counters use keyed hashes rather than storing email or network identifiers in plaintext. A short-lived essential device cookie supports abuse protection; it is not an advertising identifier.</p><h2 className="text-2xl font-serif">Service providers and retention</h2><p>Hosting, database, authentication, configured email delivery and optional AI services process the information needed for those functions. Saved records remain until deleted through supported controls or handled through a verified support request. Backup retention and any legally required retention need to be considered separately; this page does not promise immediate removal from every backup.</p>\n')]},
'src/components/auth/sign-in-form.tsx': {'sha':'63be838e44ebb78e1871e618649323bd9bfe1af6fd1be3c342369c321ab6b5c4','edits':[(2,2,'import Link from "next/link";\n'),(46,46,'      <p className="text-sm"><Link href="/forgot-password" className="inline-flex min-h-11 items-center underline">Forgot your password?</Link></p>\n')]},
'src/components/auth/sign-up-form.tsx': {'sha':'618c5200f02b5574fd851822bbb07afed34a380ce4773651d0f8cfac67304568','edits':[(8,8,'import { PASSWORD_MIN_LENGTH, PASSWORD_HELP } from "@/lib/security/password-policy";\n'),(45,47,'        <Label htmlFor="password">Password (15+ characters)</Label>\n        <Input id="password" name="password" type="password" required minLength={PASSWORD_MIN_LENGTH} maxLength={72} aria-describedby="signup-password-help" className="mt-2" autoComplete="new-password" />\n'),(48,48,'      <p id="signup-password-help" className="text-xs text-muted-foreground">{PASSWORD_HELP}</p>\n')]},
'src/components/plan/ai-assistant-drawer.tsx': {'sha':'291c6fcc3a813e02b3e4326f641cc3d4f58205fe981fee0a670e2a04866cc11b','edits':[(82,83,'    "What does my entered liquid volume mean?",\n'),(102,103,'    scrollRef.current?.scrollTo({ top: scrollRef.current.scrollHeight, behavior: window.matchMedia("(prefers-reduced-motion: reduce)").matches ? "auto" : "smooth" });\n'),(119,120,'    if (!text.trim() || pending || text.length > 1200) return;\n'),(128,129,'        body: JSON.stringify({ plan, messages: next.slice(-8) }),\n        signal: AbortSignal.timeout(30000),\n'),(187,188,'            math itself, and it won&apos;t tell you how much to take or whether something is safe. Questions and numeric inputs are sent to the configured AI provider. Do not include personal or medical details.\n'),(201,202,'          <div ref={scrollRef} role="log" aria-label="Calculation explanations" aria-live="polite" className="max-h-80 overflow-y-auto px-4 py-4 space-y-3">\n'),(262,263,'              maxLength={1200}\n                value={input}\n')]},
'src/lib/ai/__tests__/guardrails.test.ts': {'sha':'1a8767b48110a137a57a24d03ae8ef08eae87ca8a18bd998747b241084bab381','edits':[(24,24,'  "How long is this good for after mixing?",\n  "When does this vial expire?",\n'),(55,56,'')]},
'src/lib/ai/guardrails.ts': {'sha':'083797b9f7d6ef3d4c2942cde2b25498aa6be50a02bfd19c8d1d33e41c6a82cd','edits':[(6,7,' * excluded from the provider context rather than treated as trusted instructions. These functions\n'),(20,22,' * ("what does concentration mean" and "what changes when volume increases") answerable.\n'),(24,24,'  { re: /\\b(expir(?:e|es|ation|y)|shelf[- ]life|how long.*(?:good|stable|last))\\b/i, label: "storage-duration" },\n'),(53,54,'    "To change a number, edit your plan and review the new result.",\n'),(86,87,'    .replace(/[\\x00-\\x1f\\x7f]+/g, " ")\n')]},
'src/lib/auth-actions.ts': {'sha':'b05f329104cc5856aa269700dc3ac21f7f7e85896ac6b29161ff30cd706af01b','edits':[(5,5,'import { newPasswordSchema, PASSWORD_HELP } from "@/lib/security/password-policy";\nimport { enforceLimit, requestIdentity } from "@/lib/security/rate-limit";\n'),(8,11,'  name: z.string().trim().min(1).max(120),\n  email: z.string().trim().email().max(254),\n  password: newPasswordSchema,\n'),(20,21,'    return { ok: false, error: `Check your name and email. ${PASSWORD_HELP}` };\n'),(23,23,'  try {\n    await enforceLimit("signup-device", await requestIdentity(), 10, 60 * 60 * 1000);\n    await enforceLimit("signup-email", email.toLowerCase(), 3, 60 * 60 * 1000);\n  } catch { return { ok: false, error: "Account creation is temporarily unavailable. Please retry later." }; }\n'),(27,28,'  const hashed = await bcrypt.hash(password, 12);\n'),(30,31,'  try { await prisma.user.create({\n'),(38,38,'  } catch { return { ok: false, error: "Account creation could not finish. Try signing in or recovering your account." }; }\n'),(47,48,'  const email = String(formData.get("email") || "").trim().toLowerCase();\n')]},
'src/lib/auth.ts': {'sha':'01db9273398d6ab4e5cfa5fcb8d170e49fed5a7f3e83d6a5d31685db35fedefd','edits':[(7,7,'import { enforceLimit, requestIdentity } from "@/lib/security/rate-limit";\n'),(10,12,'  email: z.string().trim().email().max(254),\n  password: z.string().min(6).max(72).refine(v => Buffer.byteLength(v, "utf8") <= 72),\n'),(36,36,'        try {\n          await enforceLimit("signin-device", await requestIdentity(), 120, 15 * 60 * 1000);\n          await enforceLimit("signin-account", email.toLowerCase(), 10, 15 * 60 * 1000);\n        } catch { return null; }\n'),(48,48,'          authVersion: user.authVersion,\n'),(68,69,'          where: { id }, select: { id: true, role: true, name: true, image: true, authVersion: true },\n'),(70,71,'        const verifiedVersion = user && "authVersion" in user ? user.authVersion : undefined;\n        if (!stored || (typeof verifiedVersion === "number" && verifiedVersion !== stored.authVersion) || (!user && (token.authVersion ?? 0) !== stored.authVersion)) {\n          token.id = undefined; token.sub = undefined; token.authVersion = -1;\n          return token;\n        }\n        token.authVersion = stored.authVersion;\n'),(77,77,'      if (!token.id) return { ...session, user: undefined } as unknown as typeof session;\n')]},
'src/lib/calc/index.ts': {'sha':'35ca48ba733b6adcfee8c0652a9a650a0d18f7d49d1248386e91422839f810cb','edits':[(43,44,'    description: "U-100 scale, 0.3 mL capacity. Check the actual graduation spacing.",\n'),(52,53,'    description: "U-100 scale, 0.5 mL capacity. Check the actual graduation spacing.",\n'),(61,62,'    description: "U-100 scale, 1 mL capacity. Graduation spacing varies by product.",\n'),(79,80,'    description: "mL scale, 3 mL capacity. Confirm the scale on the actual device.",\n')]},
'src/lib/contact-actions.ts': {'sha':'9e171d17ec298ffeedb49aa795fffaf9fe3963a6fed42ebe4fea185e6dc3bb03','edits':[(1,1,'import { enforceLimit, requestIdentity } from "@/lib/security/rate-limit";\n'),(13,13,'    await enforceLimit("contact-device", await requestIdentity(), 10, 15 * 60 * 1000);\n    await enforceLimit("contact-email", email.toLowerCase(), 5, 15 * 60 * 1000);\n')]},
'src/lib/plan-actions.ts': {'sha':'8b1b6cdbdb4e25ed5ed7b5b4a525b516149f8f1d2a20d5d6504fde3678d819dc','edits':[(2,2,'import { enforceLimit, requestIdentity } from "@/lib/security/rate-limit";\n'),(74,74,'  try { await enforceLimit("plan-save", await requestIdentity(), 30, 15 * 60 * 1000); }\n  catch { return { ok: false as const, error: "Saving is temporarily limited. Your calculation is still available; retry later." }; }\n')]}
}
for path, change in changes.items():
    p = Path(path)
    raw = p.read_bytes()
    if hashlib.sha256(raw).hexdigest() != change['sha']:
        raise RuntimeError('Baseline moved: ' + path)
    lines = raw.decode().splitlines(keepends=True)
    for start, end, replacement in reversed(change['edits']):
        lines[start:end] = replacement.splitlines(keepends=True)
    p.write_text(''.join(lines))
print('Reviewed source edits applied. No production data accessed.')
