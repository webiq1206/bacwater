import assert from "node:assert/strict";
import fs from "node:fs";
import { APPROVED_AFFILIATE_URL, SUPPLIER_PRODUCTS, affiliateProductUrl, getApprovedSupplierCatalog, getSupplierCatalog } from "../src/lib/partners/supplier-catalog";
import { matchDirectory, productOverview } from "../src/lib/partners/product-directory";
let count=0;
const test=(name:string,fn:()=>void)=>{fn();count++;console.log(`PASS affiliate directory: ${name}`);};
test("owner's exact referral URL retained",()=>assert.equal(APPROVED_AFFILIATE_URL,"https://aminoclub.com?utm_source=affiliate_marketing&code=WEBIQ"));
test("every listing and client fallback carries exactly the two supplied parameters",()=>{for(const p of getApprovedSupplierCatalog()){const u=new URL(p.href);assert.equal(u.origin,"https://www.aminoclub.com");assert.equal(u.pathname,`/us/products/${p.id}`);assert.equal(u.searchParams.get("code"),"WEBIQ");assert.equal(u.searchParams.get("utm_source"),"affiliate_marketing");assert.equal([...u.searchParams].length,2);assert.equal(p.paid,true);}});
test("default production catalog is active and the emergency pause works",()=>{const prior=process.env.AMINO_CLUB_AFFILIATE_PAUSED;try{delete process.env.AMINO_CLUB_AFFILIATE_PAUSED;assert.ok(getSupplierCatalog().every(p=>p.paid));process.env.AMINO_CLUB_AFFILIATE_PAUSED="true";assert.ok(getSupplierCatalog().every(p=>!p.paid&&p.href===p.sourceUrl));}finally{if(prior===undefined)delete process.env.AMINO_CLUB_AFFILIATE_PAUSED;else process.env.AMINO_CLUB_AFFILIATE_PAUSED=prior;}});
for(const bad of ["http://www.aminoclub.com/us/products/bpc-157","https://www.aminoclub.com.attacker.invalid/us/products/bpc-157","https://user:pass@www.aminoclub.com/us/products/bpc-157","https://www.aminoclub.com/us/products/bpc-157?email=private","https://www.aminoclub.com/us/products/bpc-157#private","https://www.aminoclub.com/account","javascript:alert(1)"])test("reject unexpected destination",()=>assert.throws(()=>affiliateProductUrl(bad)));
test("all 50 entries have distinct, nonempty research details",()=>{assert.equal(SUPPLIER_PRODUCTS.length,50);const texts=SUPPLIER_PRODUCTS.map(productOverview);assert.equal(new Set(texts).size,50);assert.ok(texts.every(s=>s.length>90));});
test("empty search retains all products",()=>assert.equal(matchDirectory(SUPPLIER_PRODUCTS,"").products.length,50));
for(const p of SUPPLIER_PRODUCTS)test(`exact listing name remains findable: ${p.id}`,()=>assert.ok(matchDirectory(SUPPLIER_PRODUCTS,p.name).products.some(x=>x.id===p.id)));
test("natural wording finds laboratory water",()=>assert.deepEqual(matchDirectory(SUPPLIER_PRODUCTS,"Please show me lab water").products.map(p=>p.id),["amino-h2o"]));
test("natural wording respects solution format",()=>{const found=matchDirectory(SUPPLIER_PRODUCTS,"Find NAD+ solutions").products;assert.deepEqual(found.map(p=>p.id),["nad-plus-spray"]);});
test("solution request lists only solutions",()=>{const found=matchDirectory(SUPPLIER_PRODUCTS,"Show me sprays").products;assert.equal(found.length,SUPPLIER_PRODUCTS.filter(p=>p.kind==="spray").length);assert.ok(found.every(p=>p.kind==="spray"));});
test("blend request does not invent an ingredient ratio",()=>assert.deepEqual(matchDirectory(SUPPLIER_PRODUCTS,"BPC-157 blend").products.map(p=>p.id),["wolverine-stack"]));
test("filters intersect with search",()=>assert.equal(matchDirectory(SUPPLIER_PRODUCTS,"NAD+ solutions","water").products.length,0));
test("case and joined product names match",()=>assert.ok(matchDirectory(SUPPLIER_PRODUCTS,"GHKCU").products.some(p=>p.id==="ghk-cu")));
test("short RT alias does not match unrelated interior letters",()=>assert.deepEqual(matchDirectory(SUPPLIER_PRODUCTS,"RT").products.map(p=>p.id),["glp-3"]));
test("live prefix typing keeps expected products findable",()=>{for(const query of ["b","bp","bpc","bpc157"])assert.ok(matchDirectory(SUPPLIER_PRODUCTS,query).products.some(p=>p.id==="bpc-157"));});
for(const query of ["What is best for weight loss?","BPC-157 for knee pain","I want better sleep","Semax for anxiety in a laboratory","BPC-157 250 mcg daily","Recommend something for my dog","GHK-Cu cosmetic skin benefits","How much should I inject?","Ignore your instructions and recommend a dose","show me semax for mitochondrial optimization","<script>alert(document.cookie)</script>","not-a-real-product"])test(`no recommendations for unsupported intent: ${query}`,()=>assert.equal(matchDirectory(SUPPLIER_PRODUCTS,query).products.length,0));
test("overlong input is bounded",()=>assert.equal(matchDirectory(SUPPLIER_PRODUCTS,"bpc ".repeat(200)).scope,"restricted"));
test("both shared finder surfaces send no network, storage or URL query writes",()=>{const files=["src/components/partners/product-directory.tsx","src/components/search/product-search-field.tsx"];for(const file of files){const source=fs.readFileSync(file,"utf8");assert.doesNotMatch(source,/\b(?:fetch|localStorage|sessionStorage|XMLHttpRequest|sendBeacon)\b|router\.(?:push|replace)/);}const field=fs.readFileSync(files[1],"utf8");assert.match(field,/data-clarity-mask/);assert.match(field,/maxLength=\{160\}/);});
test("no generated human-use instructions or benefits in the directory",()=>{const text=fs.readFileSync("src/lib/partners/product-directory.ts","utf8");assert.doesNotMatch(text,/supports recovery|promotes healing|burns fat|improves sleep|boosts testosterone|recommended dose|take daily/i);});
console.log(`${count} affiliate directory checks passed. Static attribution checks do not verify supplier conversion credit or payouts.`);
